=======
Install
=======

To install EQSN, clone the repository to your drive. Then open a terminal,
go into the cloned directory, and execute the command:

```
pip install .
```

Afterwards, the EQSN package is installed on your system.
